name: animated_text_effects
description: "A Flutter plugin for text with multiple animation effects: fade, gradient, wave, typewriter, bounce, shimmer, slide, blur."
version: 0.0.1
homepage:

environment:
  sdk: ^3.11.5
  flutter: '>=3.3.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
